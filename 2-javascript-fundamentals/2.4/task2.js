
alert(1);
// When should we use capitals for a constant and when should we name it normally? 
/*Being a “constant” just means that a variable’s value never changes.*/
//constants that are known prior to execution
const COLOR_RED = "#F00";
const BIRTHDAY ="18.02.2000";
alert(BIRTHDAY);
alert(age);
/*The value of pageLoadTime is not known prior to the page load, so it’s named normally.*/

//const pageLoadTime=/* time taken by a webpage to load */;
let value=19;
age=someCode(value);

someCode(value)
{
    alert(20);
}

//in this age is calculated at run time